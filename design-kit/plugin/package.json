{
  "name": "@powers/figma-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Powers Design Kit Figma plugin — sync variables, audit catalog, stub missing components.",
  "license": "BUSL-1.1",
  "author": "Scott Powers",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.109.0",
    "esbuild": "^0.25.1"
  }
}
