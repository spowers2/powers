# Copy to .env.local (gitignored). Never commit real secrets.

# Figma REST API — read Powers UI Kit (see design-kit/FIGMA.md)
# PAT scopes (Settings → Security → Personal access tokens):
#   - File content: Read
#   - File variables: Read   ← required for variables export / full audit
FIGMA_FILE_KEY=bdfYWkMm5oJqKBIrwWCsSd
FIGMA_ACCESS_TOKEN=
