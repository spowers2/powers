# Copy to .env.local (gitignored). Never commit real secrets.

# Figma REST API — read Powers UI Kit (see design-kit/FIGMA.md)
# PAT UI scopes:
#   - "Read the contents of and render images from files"  (file_content:read)
#   - Code Connect: Write  (for `pnpm code-connect:publish` — Org/Enterprise)
# Variables dump via API needs Enterprise scope file_variables:read (often not shown).
FIGMA_FILE_KEY=bdfYWkMm5oJqKBIrwWCsSd
FIGMA_ACCESS_TOKEN=
